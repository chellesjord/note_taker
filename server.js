//initiate express
const express = require('express');
const app = express();
// const PORT =
// const fs = require('fs');
// const util = require('util');

app.listen(3001, () => {
    console.log(`API server now on port 3001!`);
  });

//   const { }